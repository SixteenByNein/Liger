## Liger

![](icon.png)

Liger is a light-weight static site generator. The idea is to give the developer access to the benefits of a static site generator (such as using elements across multiple pages without needing to update each instance manually) without getting in the way of the developer writing simple HTML.